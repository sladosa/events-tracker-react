[build]
  command = "npm run build"
  publish = "dist"

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["docs/help/**"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
