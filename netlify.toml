[build]
  command = "npm run build"
  publish = "dist"

[dev]
  framework = "vite"
  port = 8888
  autoLaunch = false

[functions]
  directory = "netlify/functions"
  node_bundler = "esbuild"
  included_files = ["docs/help/**"]

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
